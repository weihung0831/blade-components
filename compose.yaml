services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - 127.0.0.1:${PORT:-3200}:80
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
